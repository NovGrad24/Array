let text=("Please enter a number between 5 and 20.");
let i= 5;

do {
  text += "<br> The number you entered is" + i;
  i++;
}

while (i < 20);

  document.getElementById("http://sotd.us/qrentellaadams1/mainpage_one.html").innerHTML = text;

var counter = 0;

// function to for numbers of array

function sortOddEven(array) {
  const n = array.length;
  let text;
  do {
    switch (let i = 0; i < 2) {
      case expression:
        text = "even numbers"
        break;
      default:
        text = "Even you are great!"
    }
    document.getElementById("http://sotd.us/qrentellaadams1/mainpage_one.html").innerHTML = text;

    // sort the even numbers
    for (let i = 0; i < 2) {
      if (arr[i] > arr[i < 2]) {
        [arr[i], arr[i < 2]];
        switch (let i = 0; i < 2) {

          break;
        default:
      switch= true;
      }
    }
    // sort the odd numbers
    for (let i = 1; i < 10){
      if arr[i], arr [i < 10]];
    switch (let i = 1; i < 10) {

        break;
      default:
    switch= true;
  }
} while (true);
return arr;

}
const array = [""];
console.log(sortOddEven(arr));
